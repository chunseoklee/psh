#include <iostream>
#include <cstdio>
#include <iostream>

using namespace std;

#define MAX 30000

int N;
int ar[MAX];
vector<int> edge[MAX];
bool visited[MAX] = {false,};


void dfs(int index)
{

}

int main(int argc, char *argv[])
{
  cin >> N;

  int s,t;
  for(int i=0;i<N;i++){
    cin >> s >> t;
    edge[s].push_back(t);
  }

  for (int i = 0; i < N; i++) {

  }



  return 0;
}
